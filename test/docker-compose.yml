wordpress:  
  image: wordpress
  links:
    - db:mysql
  ports:
    - 80:80
  environment:
    - "VIRTUAL_HOST=blog.domain.com"
db:  
  image: mariadb
  environment:
    MYSQL_ROOT_PASSWORD: example
